═══════════════════════════════════════════════════════════════
🚀 КОМАНДЫ ДЛЯ РАЗВЕРТЫВАНИЯ v3.0.0 НА СЕРВЕРЕ
═══════════════════════════════════════════════════════════════

📋 КОПИРУЙ ВСЮ КОМАНДУ ЦЕЛИКОМ И ВСТАВЬ В SSH:
───────────────────────────────────────────────────────────────

cd /opt/sofiya/backend && git fetch --tags && git checkout v3.0.0 && sudo systemctl restart sofa-backend && echo "✅ Backend обновлен до v3.0.0!" && sudo systemctl status sofa-backend --no-pager && echo "" && echo "📋 Последние 20 строк логов:" && sudo journalctl -u sofa-backend -n 20 --no-pager


═══════════════════════════════════════════════════════════════
📋 ИЛИ ПО ШАГАМ:
───────────────────────────────────────────────────────────────

cd /opt/sofiya/backend
git fetch --tags
git checkout v3.0.0
sudo systemctl restart sofa-backend
sudo systemctl status sofa-backend
sudo journalctl -u sofa-backend -n 30 --no-pager


═══════════════════════════════════════════════════════════════
✅ ПОСЛЕ РАЗВЕРТЫВАНИЯ ПРОВЕРЬ:
───────────────────────────────────────────────────────────────

1. Версия:
   cd /opt/sofiya/backend && git describe --tags

2. Статус сервиса:
   sudo systemctl status sofa-backend

3. Ошибки в логах:
   sudo journalctl -u sofa-backend -n 50 | grep -i error

4. Тестирование в ChatGPT:
   - wordstat_get_user_info
   - wordstat_get_regions_tree (ИСПРАВЛЕН!)
   - wordstat_get_top_requests
   - wordstat_get_regions (ИСПРАВЛЕН!)
   - wordstat_get_dynamics


═══════════════════════════════════════════════════════════════
🎉 ГОТОВО!
───────────────────────────────────────────────────────────────

Версия 3.0.0 развернута на сервере!
Все 5 методов Wordstat API теперь работают!

═══════════════════════════════════════════════════════════════

